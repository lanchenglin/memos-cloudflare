# Agent 项目入口

本仓库是 `lanchenglin/memos-cloudflare`，面向个人的图文笔记，当前文档基线为 0.3.0。

## 文档导航

- 日常使用：`docs/USER_GUIDE.md`。
- Hermes 主动配置与部署：**`docs/HERMES_DEPLOY.md`**，执行部署前完整阅读，不只读取摘要。
- 人工部署：`docs/DEPLOY.md`。
- 导出与灾备：`docs/BACKUP.md`。
- 已执行的开发测试范围：`docs/TEST_REPORT.md`。
- 安全要求：`SECURITY.md`。

## 工作范围

仅在用户明确要求部署、升级或维护本项目时，按 Hermes 任务书执行相应操作。仅加载本文件不应触发生产写入。

用户已授权的常规操作应主动完成，不只返回操作建议。缺少凭据、账单开通、无法消除的目标账号歧义或危险的旧数据迁移时，给出已完成部分和最小阻塞项。文档不替代现有工具权限或安全审批；不要修改 Hermes 全局审批设置。

## 必须保持的架构和数据约束

- 单 Worker 同源托管网页和 API；D1 绑定 `DB`，私有 R2 绑定 `R2`，静态资源绑定 `ASSETS`。
- 域名绑定 Worker，不公开 R2，不用放宽 CORS、公开笔记或关闭鉴权来修复图片。
- 使用当前仓库锁定的 npm 依赖和 Wrangler；Node.js 24+，备份/测试需要 Python 3.10+。
- 生产命令统一使用任务书的本机专用配置。不要误用带零 UUID 的模板，也不要改数据库名称后继续运行硬编码示例名称的脚本。
- 已有资源、账号和 JWT_SECRET 保留；重复部署不是重新建库、重新注册或轮换密钥。
- 先发现并核对归属，再创建。没有明确目标域名时使用实际 workers.dev 入口，不接管其他站点。
- 已有数据升级前备份并审核迁移，不删表、删桶、伪造迁移记录、强推或覆盖用户本地修改。
- 密码、Token、Cookie、生产配置和备份保存到仓库外受限位置，不能进入公开 Git、普通日志或回复。

## 验收和报告

根目录常规检查：`npm run setup`、`npm run check`、`npm test`、`npm run build`。

`npm run test:browser` 是本地模拟环境测试，不代表真实 Cloudflare 站点通过。部署必须继续验证实际域名的登录、图文保存、刷新、中文搜索、私有图片匿名拒绝和图文备份。

网页 JSON/Markdown 导出不包含原图；`scripts/backup.py` 便携备份不等于整站灾备；内置 Cron 不是自动备份。

报告实际 Git SHA、Worker 最终版本、访问地址、账号用户名、密码安全保存位置、逐项 PASS/FAIL/NOT_RUN 和剩余问题。没有真实验证不报成功，不改写历史测试报告冒充生产验收。

旧 Pages/前后端分离文档不适用于本版。接口以当前源码为准；CLI 参数先核对项目安装版本的 `--help`。
